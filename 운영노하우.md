
## §19. 쿠팡 정산 API 실측 (2026-09-02)
- **매출내역** `GET /v2/providers/openapi/apis/api/v1/revenue-history` — 필수: vendorId, recognitionDateFrom/To(**종료일은 어제까지**, 최대 31일), maxPerPage, **token(첫 페이지는 빈 문자열 `""` — 파라미터 자체가 없으면 400 "token cannot be null")**. 응답: orderId, saleType(SALE/REFUND), recognitionDate(배송완료+7일 또는 구매확정), vendorItemId, saleAmount, **serviceFee·serviceFeeVat·serviceFeeRatio(실수수료)**, settlementAmount, deliveryFee{...}. 페이징 hasNext/nextToken.
- **지급내역** `GET /v2/providers/marketplace_openapi/apis/api/v1/settlement-histories?revenueRecognitionYearMonth=YYYY-MM` — 문서 표기(v2/vendor/...)와 실제 경로가 달라 404 났음, 위 v1 경로가 정답(실측 200). 응답: settlementType(WEEKLY/MONTHLY/ADDITIONAL/RESERVE), settlementDate, totalSale, serviceFee, settlementAmount(주간 70%), lastAmount(30% 이연), **finalAmount(지급예정액)**, deductionAmount, pendingReleasedAmount, status(DONE/SUBJECT).
- 모듈 `쿠팡정산.py` → `정산현황.json` → 관제탑 '쿠팡 정산 실측' 카드. 감시루프 일 1회. 2026-09-02 현재 인식 매출 0건(첫 판매 8/24, 구매확정·인식 대기) — 데이터 들어오면 추정 수수료 11.55%와 실수수료 비교, 반품 건 반품배송비 정산 여부 확정.
- 애널리틱스(조회수·전환율) API는 쿠팡·네이버 모두 없음(공식 카테고리 전수 확인). 화면 캡처만 가능.
